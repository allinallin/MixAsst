/**
 * 
 * USER CONFIG VALUES
 * 
 * These values are required by the app
 * and should not be committed to a public 
 * repository.
 * 
 */

module.exports = (function() {
	var userConfig = {
		ECHONEST_KEY: ''
	}
	return userConfig;
})();